<%= form.input :zipcodes, 
               as: :type_picker, 
               wrapper: false,
               collection: Zipcodes.paris.map{|zipcode| [zipcode, zipcode]}, 
               multiple: multiple %>
