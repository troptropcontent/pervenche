en:
  models:
    service:
      attributes:
        kind:
          enum:
            pay_by_phone: "PayByPhone"
            easy_park: "Easy Park"
      validations:
        credentials: "invalid. The email or password provided seems not to be correct."
